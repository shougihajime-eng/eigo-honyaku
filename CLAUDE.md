# 英語翻訳（将棋YouTube字幕ツール）

このファイルは、このプロジェクトの設計メモ。Claude Code が会話のたびに自動で読む。

---

## 🔒 永続ルール（絶対遵守・毎回これを守る）

このプロジェクトの**テロップ／字幕**を扱うすべての作業で、ユーザーから明示の指示が無くても**最初から以下を守る**。
コード変更・プリセット追加・新規画面追加・既定値変更・翻訳プロンプト調整など、テロップに触れる全作業で適用。

### テロップ配置の永続ルール

1. **黒帯部分（画面下端の暗い領域）に収まる場合は、必ずそこに配置する**
2. **黒帯が無い場合は、画面下部の見やすい位置に配置する**（下端から余白 4〜8% 目安）
3. **中央配置は禁止**（読みづらい・画面を遮る）。`align: "middle"` を既定値にしない／UIで目立たせない
4. **見やすさを最優先する**（フォントサイズ・コントラスト・縁取り・帯背景でハッキリ見せる）
5. 人物の顔・将棋盤・重要な局面に重ねない
6. 背景と同化する色は禁止（白背景に白文字、黒背景に黒文字 など）

→ コード上の既定値は `lib/telop/defaults.ts` の `DEFAULT_STYLE`（黒帯＋白文字・下部配置）。
→ 新しい既定値・新規プリセットを追加する場合も、上記6点を満たすこと。

---

## 📍 進捗（いまここ）

- ✅ **済み**:
  - デザイン方針を受信／設計メモ（このファイル）を作成
  - **クイック翻訳機能を実装完了**（Claude Haiku、4モード、将棋辞書 100+ 語、スマホ対応）→ `/quick`
  - 動画字幕パイプラインの技術選定 → **品質最優先構成で確定**
  - **テロップ追加要件を受信**（2026-05-12、SRTだけでなく動画に乗せる「見やすい英語テロップ」も書き出す）
  - **動画字幕の基本パイプラインを実装完了**（YouTube URL → Google Speech 書き起こし → Sonnet 翻訳＋二重チェック → 字幕確認・編集 → SRT 出力 → ffmpeg で MP4 焼き込み）→ `/video`
  - **テロップ スタイル調整UI＋実動画プレビューを実装完了**：
    - 4種のプリセット（白＋黒フチ／黒帯／黄色＋黒フチ／盤を隠さない小帯）
    - フォントサイズ・色・太さ・位置・余白・行数・縁取り・帯背景までスライダー/カラピで調整
    - 確認画面に **実動画プレイヤー＋リアルタイム字幕重ね** を追加（焼き込み後とほぼ同じ見た目で確認可）
    - **品質警告**（英文長すぎ・行数オーバー・表示時間短すぎ・翻訳要確認）が自動で各セグメントに表示
  - **テロップ設計プレイグラウンド**（`/telop`）：将棋盤モック背景の上でテロップ設定を試せる単独ページ
  - **プロジェクトの保存・読み込みを実装完了**：
    - 編集中は **自動下書き保存**（localStorage、ページを閉じても次回起動時に「続きから開く」が出る）
    - 「プロジェクトを保存」ボタン → JSONファイル（`.eigo-honyaku.json`）で書き出し
    - 入口で「保存したプロジェクトファイルを開く」→ jobId が生きていれば動画プレビュー込みで完全復元、消えていれば URL から再生成を促す
    - 「SRTだけ書き出す」ボタン（焼き込み不要・即ダウンロード、クライアント側で生成）
    - 動画ファイル消失時は **動画なしモード** に自動切替（編集と SRT 書き出しのみ）
  - yt-dlp と ffmpeg を winget で導入済み
  - **`/subtitle` Vercel 対応版を新規実装完了**（2026-05-12）
    - MP4 を選ぶだけで動く（YouTube DL 不要、ffmpeg バイナリ不要）
    - ブラウザ内で `ffmpeg.wasm` が音声抽出 → API へ送信 → Vercel Serverless 上で完結
    - 既存の Speech v2 + Sonnet 4.6 二重チェック + テロップエディタを再利用
    - 8分以下の動画に限定（同期 API 10MB 上限のため。長尺は次フェーズ）
    - 出力は **日本語SRT + 英語SRT + テロップJSON の3ファイル**（焼き込みは Phase2）
    - `npm run build` 通過済み、Vercel 公開準備完了
  - **`/video` を長尺対応に拡張完了**（2026-05-12）
    - 音声が 9MB を超えたら ffmpeg で 4 分ごとに自動分割
    - 各チャンクを Google Speech v2 同期API に順番に投げ、タイムスタンプを合算して結合
    - **GCS の追加設定なしで 10〜30分の動画に対応**（GCS+batchRecognize は将来用）
    - `lib/video/speech.ts` の `transcribeJapanese` 内で透過的に処理（呼び出し側コード変更なし）
  - **全ページのスマホ/タブレット対応＋黒テーマ統一を実施**（2026-05-12）
    - `/subtitle` `/telop` が「黒地に黒文字」「白地に白文字」で読めなかった問題を解消
    - 黒テーマに統一し、iPhone/Android/iPad すべてで正常表示
    - iPhone のセーフエリア・自動ズーム防止・横スクロール防止を追加
    - 主要ボタンを最低 44px に拡大（指でタップしやすく）
  - **テロップ配置ルールを永続化**（2026-05-12）
    - CLAUDE.md 上部に「永続ルール」セクションを追加（黒帯優先・中央禁止・下部・見やすさ最優先）
    - `lib/telop/defaults.ts` の DEFAULT_STYLE コメントを強化
    - 今後はユーザーから明示の指示が無くてもこのルールを守る
  - **翻訳品質の徹底強化（誤訳ゼロ計画）**（2026-05-12）
    - **秒読みカウントダウン演出**: 「1, 2, 3...」を自動検出 → 翻訳せず色付き数字（10〜6白／5〜3黄／2〜1赤）で表示
    - **翻訳前の固有名詞確認画面**: 書き起こし直後に Claude が棋士名・戦法・棋戦・段位を抽出 → ユーザーが英訳を確認・修正してから翻訳に進む
    - **書き起こし確認・修正ステップ**: 動画を見ながら日本語の文字起こしを直接編集できる新ステップ。誤訳の元から断つ
    - **逆翻訳チェック**: 翻訳結果を日本語に戻して原文と比較。意味乖離を警告（レビュー画面に表示）
    - **辞書の自動学習（Supabase）**: 「固有名詞確認」で確定した英訳を `eigo_honyaku.user_dictionary` に保存 → 次回以降の動画で自動適用
    - 翻訳プロンプトを将棋界専門ローカライズに書き換え（推測禁止・段位維持・JSF表記優先・AI装飾禁止）
    - 鈴木肇 → Hajime Suzuki を辞書に固定
- 🟡 **進行中**: なし
- 🔜 **次の一歩**:
  1. **新しい動画で5機能まとめて実機検証**（書き起こし確認→固有名詞確認→翻訳→逆翻訳→辞書学習）
  2. **本番に反映＆Vercelに Supabase 環境変数を登録**（GitHub に push 済み・要 `NEXT_PUBLIC_SUPABASE_URL` と `NEXT_PUBLIC_SUPABASE_ANON_KEY` をVercelにも追加）
  3. 1時間級の超長尺対応 = Speech v2 batchRecognize + GCS 経由（次フェーズ）

### 🗄️ 共有 Supabase 設定（2026-05-12 完了）

- **スキーマ**: `eigo_honyaku`
- **テーブル**: `user_dictionary` （jp UNIQUE / en / category / used_count）
- **RLS**: 単一ユーザー前提の緩めポリシー（SELECT/INSERT/UPDATE 全許可）
- **PostgREST 公開**: `eigo_honyaku` を Exposed schemas に追加済み
- **Migration**: `supabase/migrations/20260512_user_dictionary.sql`
- **環境変数**: `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY`（本番 Vercel にも要登録）

### ✅ 公開完了 (2026-05-12)

- **本番URL**: https://eigo-honyaku.vercel.app
- **GitHub**: https://github.com/shougihajime-eng/eigo-honyaku
- **Vercel環境変数**:
  - `ANTHROPIC_API_KEY`（Production+Preview）✅ 動作確認済
  - `GOOGLE_APPLICATION_CREDENTIALS_JSON`（Production+Preview）✅ セット済
  - `GOOGLE_CLOUD_PROJECT`=`shogi-subtitle-jct4k`（Production+Preview）✅ セット済
  - ※ Vercel CLI の `env add` でstdin経由だと値が空保存される不具合あり。**Vercel REST API の PATCH 経由で更新する**（auth.json のトークンで認証）
- **動作確認済み**: `/api/translate` 200応答、将棋辞書（居飛車穴熊・角・王手・腰掛け銀など）の英訳適用OK

---

## 🎯 このアプリの目的

**将棋YouTuberが、最小操作で、美しく、見やすく、誤訳の少ない英語字幕動画を作れること**

「翻訳AI」ではない。
機能量より「完成度」。派手さより「使いやすさ」。

---

## 👥 想定ユーザー

- 将棋系YouTuber（プロ／アマ問わず）
- 動画編集の専門家ではない
- 毎日この作業をやる人もいる（＝疲れにくいUIが必須）

---

## 🖥 対応端末（すべて完全対応）

- iPhone
- Android
- iPad
- PC

特に **スマホ使用を重視**。レスポンシブを徹底し、どの画面サイズでも崩れないこと。

---

## 🎨 UI・デザイン最重要方針（絶対遵守）

これは毎日使う実用ツール。動くだけでは不十分。

### 必ず満たすこと

- 見やすい
- 迷わない
- 気持ちいい
- 疲れない
- 間違えにくい

### 見た瞬間に感じてほしい印象

- 綺麗
- わかりやすい
- 触りたくなる
- プロっぽい
- ミスしにくい

### やってはいけないこと（絶対条件）

- ごちゃごちゃさせない
- ボタンを増やしすぎない
- 難しい専門設定を最初から見せない
- 初心者が迷子にならない
- 文字を小さくしない
- 情報を詰め込みすぎない
- 色を使いすぎない

### 理想の操作感

> 「動画を入れたら、気持ちよく、自然に、迷わず、字幕完成まで進める」

操作していてストレスを感じないことを最優先。

---

## 📺 最重要画面：字幕確認画面

**このアプリの心臓部**。ここがダメなら全部ダメ。

### 一瞬で確認できるべき4つの情報

1. 日本語（元の文）
2. 英語（翻訳結果）
3. 将棋用語（正しい訳になっているか）
4. 誤訳警告

### 重要用語の表示方法（見落とし防止）

- 色分け
- 強調表示
- 辞書アイコン
- 警告表示

### 視認性の条件（長時間作業しても疲れない）

- 行間
- 余白
- 文字サイズ
- コントラスト

→ **プロの編集者が毎日使いたくなるレベル**を目指す。

---

## 📛 翻訳の絶対ルール（将棋界専門編集者として）

これは一般翻訳ではなく **「将棋YouTube動画の海外向けローカライズ」**。
正確性を「自然な英語」より優先する。コードの翻訳プロンプト（`lib/video/translate.ts`）と辞書（`lib/shogi-dictionary.ts`）に反映済み。

### 最重要：固有人名

| 日本語 | 英語 | 禁止表記 |
|---|---|---|
| 鈴木肇 | Hajime Suzuki | 鈴木始／鈴木元／Suzuki Hajimu／その他の勝手な変換 |

→ チャンネル運営者ご本人の名前。**絶対に1文字も変えない**。
→ 辞書 (`lib/shogi-dictionary.ts`) の name カテゴリに登録済み。新しい棋士名を扱うときも同じ要領で追加する。

### 翻訳全体の方針

- 棋士名・戦法名・棋戦名・段位は **推測禁止**。確信がない場合は原文を残す
- 段位・称号（七段、名人、王座、竜王 など）は省略しない（例：「藤井名人」→ "Fujii Meijin"）
- 将棋専門用語は **日本将棋連盟の公式英語表記を優先**
- AIっぽい大げさな装飾・存在しない英単語の創作は禁止
- 意味を変える意訳は禁止。原文の温度感を保つ

### 翻訳前の固有名詞チェック（運用ルール）

将棋動画を翻訳する前に、以下を一覧化してから翻訳に進む：
1. 棋士名（漢字＋公式英語表記）
2. 戦法名（辞書にあるか）
3. 段位・称号
4. 棋戦名（竜王戦・名人戦 等の英語表記）
5. サムネ文字・タイトル

不明な固有名詞があれば **翻訳停止し、確認を求める**。
※ この「事前チェック画面」は次フェーズで実装予定（現状は翻訳後の警告で拾う）。

---

## 🎬 テロップ機能（追加要件 2026-05-12）

**SRT を作るだけでは不十分**。動画にそのまま乗せられる「見やすい英語テロップ」のデータも作る。
海外視聴者がスマホで一瞬で読めることが目的。

### テロップ要件（必須）

- **配置の最優先は「黒帯＋白文字」**（チャンネル統一感＋最高の視認性。`DEFAULT_STYLE` で固定）
- 黒帯に収まらない場合のみ、画面下部の見やすい位置へ配置
- **スマホ視聴**でも読める文字サイズ
- 1行が長くなりすぎないよう自動改行
- **原則2行以内**
- 1字幕あたり長すぎる英文を出さない
- 表示時間が短すぎる字幕は警告
- フォントサイズ・位置・背景・縁取りを画面で調整できる
- プレビュー画面で実際の見え方を確認できる

### 配置の禁止事項

- **画面中央**への配置（読みづらく、画面を遮る）
- **人物の顔**に重なる配置
- **将棋盤や重要な局面**を隠す配置
- **背景と同化**して読みにくい字幕（白い背景に白文字など）

→ 最優先は「**一瞬で読めること**」

### プレビューでリアルタイムに確認できること

- どう見えるか
- 小さすぎないか
- 読みやすいか
- 将棋盤を邪魔していないか

### 出力形式（最初は2種類）

1. **YouTube 用 SRT ファイル**（字幕標準形式・どこにでも貼れる）
2. **動画焼き込み用テロッププロジェクトJSON**
   - 字幕テキスト＋タイムコード＋見た目設定（フォント／色／位置／縁取り／背景）を1ファイルに保存
   - 将来 `ffmpeg drawtext` で焼き込み動画を書き出すのに使う

### スコープ

- 最初は「テロップ設定したJSONとプレビュー」までで OK
- 焼き込み動画書き出しは Phase 2（将来）
- **最初から高度な動画編集ソフトにはしない**（カット・エフェクト等は不要）

### 字幕の品質ルール（翻訳プロンプトに渡す）

字幕は「翻訳が正しい」だけではダメ。YouTube では**見やすい・読みやすい・テンポがいい**字幕でなければ意味がない。

- 1字幕は短く（半角40文字目安・絶対50文字以内）
- 自然な英語
- 句読点や改行で**読むテンポ**を作る
- 将棋専門用語は辞書通り

---

## 🛠 技術構成（確定分／予定分）

### フロント・公開
- フロント: **Next.js 14 (App Router) + React 18 + Tailwind CSS**（確定・導入済み）
- 将棋辞書: `lib/shogi-dictionary.ts` のローカル JSON（クイック翻訳で使用中・追記しやすい構造）
- 公開: Vercel（予定）
- データベース: 共有 Supabase（`~/.claude/CLAUDE.md` 参照）／スキーマ名 `eigo_honyaku`（動画字幕機能で必要になったら導入）

### クイック翻訳（実装済み）
- **Claude Haiku 4.5** (`claude-haiku-4-5-20251001`)
- 速度・コスパが効く小型機能なので Haiku 継続

### 動画字幕パイプライン（品質最優先構成・2026-05-12 確定）

| 工程 | 採用技術 | 理由 |
|---|---|---|
| 音声書き起こし | **Google Cloud Speech-to-Text v2**（latest_long モデル + カスタム辞書） | 将棋用語をカスタム辞書に登録できる。日本語精度高い |
| 一次翻訳 | **Claude Sonnet 4.6** + 将棋辞書プロンプト | 文脈理解が最高クラス。辞書を守って訳す |
| 二重チェック | **Claude Sonnet 4.6** で別プロンプト再評価（誤訳警告を出す） | 同じ翻訳を2回ちがう視点で見るので品質が上がる |
| 字幕タイミング | Google Cloud Speech が返す word-level timestamps を使用 | API 標準機能 |
| 書き出し（必須） | **SRT ファイル** ＋ **テロッププロジェクトJSON** の2種類 | SRT＝YouTube字幕／JSON＝見た目情報込みの焼き込み素材 |
| 書き出し（将来） | ffmpeg で字幕焼き込み動画 | テロッププロジェクトJSONをもとに動画書き出し |

### 必要な API キー（`.env.local` で管理）

| キー | 用途 | 状態 |
|---|---|---|
| `ANTHROPIC_API_KEY` | Claude API（クイック翻訳・動画字幕翻訳） | 🔜 `.env.local` に貼り付け |
| `GOOGLE_APPLICATION_CREDENTIALS_JSON` | Google Cloud Speech 認証（動画字幕の書き起こし） | 🔜 GCP 側でキー取得 |

### 🛠 Google Cloud セットアップ手順（非エンジニア向け・1回だけ）

動画字幕機能を使うには Google Cloud の準備が必要。**5分くらいの作業**。

1. **Google アカウントでログイン**
   https://console.cloud.google.com/ を開いて Google アカウントでログイン
2. **プロジェクトを作る**
   画面上の「プロジェクトの選択」→「新しいプロジェクト」→ 名前は何でも OK（例：`eigo-honyaku`）→ 作成
3. **Speech-to-Text API を有効化**
   左メニュー「API とサービス」→「ライブラリ」→ 検索バーに `speech` → 「Cloud Speech-to-Text API」→「有効にする」
4. **お金の設定（無料枠で十分・カード登録だけ）**
   左メニュー「お支払い」→「請求先アカウントをリンク」→ クレジットカードを登録
   ※ Google Speech は **毎月60分まで無料**。5分動画なら12本／月までタダ
5. **サービスアカウントを作る**
   左メニュー「IAM と管理」→「サービスアカウント」→「サービスアカウントを作成」
   名前：`eigo-honyaku-app` → 作成 → ロール「Cloud Speech クライアント」を付与 → 完了
6. **JSON キーをダウンロード**
   作ったサービスアカウントを選択 →「キー」タブ →「鍵を追加」→「新しい鍵を作成」→ JSON 形式 → 作成
   ※ ダウンロードフォルダに JSON ファイルが入る
7. **`.env.local` に貼り付け**
   - メモ帳で `C:\Users\shoug\英語翻訳\.env.local` を開く（無ければ新規作成）
   - ダウンロードした JSON ファイルもメモ帳で開く
   - JSON の中身を **1行にして** 以下のように書く：
     ```
     ANTHROPIC_API_KEY=sk-ant-あなたのキー
     GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account",...全部1行...}
     ```
   - 改行を取り除く必要は実はないが、わかりやすさのため `{...}` 全体を1行で。改行があってもアプリ側で扱う
8. **完了**
   `npm run dev` で起動して http://localhost:3000/video を開く

### 1時間動画あたりの想定コスト（品質最優先構成）
- 書き起こし（Google Cloud Speech 強化モデル）: 約 240 円
- 一次翻訳（Sonnet 4.6）: 約 100 円
- 二重チェック（Sonnet 4.6）: 約 100 円
- **合計：約 440 円／1時間動画**

### 起動方法（PowerShell）

```powershell
cd C:\Users\shoug\英語翻訳
npm run dev
```

→ http://localhost:3000

### ファイル構成（現状）

```
app/
  layout.tsx                          共通レイアウト
  page.tsx                            トップ（メニュー：動画字幕／クイック翻訳）
  globals.css                         スタイル
  quick/page.tsx                      クイック翻訳画面
  video/page.tsx                      動画字幕画面（YouTube URL → 完成MP4 まで一気通貫）
  api/
    translate/route.ts                クイック翻訳API（Claude）
    video/
      download/route.ts               YouTube ダウンロード（yt-dlp + ffmpeg）
      transcribe/route.ts             書き起こし（Google Speech v2）
      translate/route.ts              翻訳＋誤訳チェック（Sonnet 4.6 ×2）
      render/route.ts                 SRT 生成 → MP4 焼き込み（ffmpeg）
      file/route.ts                   完成ファイル（SRT/MP4）のダウンロード
lib/
  shogi-dictionary.ts                 将棋用語辞書 + 検出関数
  video/
    binaries.ts                       yt-dlp / ffmpeg のパス解決
    jobs.ts                           ジョブ作業フォルダ管理
    ytdlp.ts                          YouTube ダウンロード処理
    speech.ts                         Google Speech v2 呼び出し
    translate.ts                      翻訳＋校閲パイプライン
    srt.ts                            SRT 書式変換
    burn.ts                           ffmpeg 字幕焼き込み
tmp/video-jobs/<uuid>/               1ジョブごとの一時ファイル置き場（git無視）
```

### 動画字幕の処理時間目安（5分動画）

- ダウンロード: 5〜30秒
- 書き起こし: 10〜30秒
- 翻訳（一次＋二重チェック）: 30〜60秒
- 焼き込み: 30〜90秒
- **合計：おおよそ 1〜3分**

---

## 📦 クイック翻訳機能（実装済み）

YouTube運営中に「タイトル・サムネ文字・概要欄・コメント返信」などをすぐ英訳したい場面のための機能。
**動画アップロード不要・テキストだけで即翻訳**。

### 4つのモード

| モード | 用途 | プロンプト方針 |
|---|---|---|
| 自然英語 | 海外読者に自然な英語（普段使い） | 文法的に正しく、英語として滑らかに |
| YouTubeタイトル | クリックされやすい英語タイトル | 短く・インパクト・釣りすぎない |
| 将棋専門 | 解説などで用語を正確に | 海外の将棋ファンに通じる正式英訳 |
| 直訳 | 原文に忠実 | 意訳・装飾なし |

### 将棋辞書

`lib/shogi-dictionary.ts` に 100+ 語（駒・戦法・囲い・手筋・棋戦名 など）。
入力に辞書語が含まれていれば、AI に「必ずこの英訳を使え」と強制ヒントを渡す。
辞書ヒットは画面下にバッジ表示。

辞書追加は配列に1行足すだけ：
```ts
{ jp: "新しい用語", en: "English Term", category: "opening" },
```

### UI 仕様

- モード4つを上部に大きく表示（タップで切替）
- 日本語入力 → 英訳ボタン or `Ctrl + Enter`
- 結果は「コピー」ボタンで一発コピー
- スマホ縦持ちでも崩れない（max-w-3xl 中央寄せ、grid 2列→4列）

---

## 🔗 本番URL／管理画面

- **本番（Vercel）**: https://eigo-honyaku.vercel.app
  - トップ: https://eigo-honyaku.vercel.app/
  - 動画字幕生成 (Vercel対応): https://eigo-honyaku.vercel.app/subtitle
  - クイック翻訳: https://eigo-honyaku.vercel.app/quick
  - テロッププレイグラウンド: https://eigo-honyaku.vercel.app/telop
- **GitHub リポジトリ**: https://github.com/shougihajime-eng/eigo-honyaku
- **Vercel ダッシュボード**: https://vercel.com/shougihajime-3368s-projects/eigo-honyaku
- ⚠️ **動かすには環境変数の登録が必須**:
  - `ANTHROPIC_API_KEY` (クイック翻訳・動画翻訳)
  - `GOOGLE_APPLICATION_CREDENTIALS_JSON` (動画書き起こし)
  - 登録は Vercel ダッシュボード → Settings → Environment Variables

---

## 📚 主要ドキュメント

| ドキュメント | 場所 | 用途 |
|---|---|---|
| 設計メモ（このファイル） | `CLAUDE.md` | デザイン方針・進捗 |

---

## ✅ 完成度の判断基準

このアプリが「完成」と言えるかは、次の問いで判断する：

- スマホで触っても操作に迷わないか？
- 字幕確認画面で、誤訳が一瞬で見つかるか？
- 将棋用語が正しく訳されているか？
- 長時間使っても目が疲れないか？
- 「またこのツールで作りたい」と思えるか？

YES が全部揃って初めて完成。
